[pytest]
# Tests async de integración (pytest-asyncio). 'auto' = toda async def test/fixture
# se corre en su event loop sin decorar una por una.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
